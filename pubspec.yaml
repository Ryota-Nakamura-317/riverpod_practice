name: rivepod_practice2_app
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  hooks_riverpod: ^0.12.4
  flutter_hooks: ^0.15.0
  state_notifier: ^0.6.0
  simple_logger: ^1.7.0
  fluttertoast: ^7.1.6


dev_dependencies:
  freezed: ^0.12.7
  build_runner: ^1.11.1
  flutter_test:
    sdk: flutter


flutter:


  uses-material-design: true

